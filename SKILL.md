---
name: douni-lifecycle-data-hub
description: 维护和查询豆包用增部门统一的达人合作全生命周期多维表；用于达人提报、执行、脚本、审核、发布、机构进度、项目/专项、数据差异、跨表核对、来源追溯、迁移、同步、仪表盘口径或数据准确性检查。任何业务 Skill 需要读取达人生命周期事实时，先使用本 Skill 获取统一口径。
---

# 豆尼全生命周期数据中枢

## 当前事实层

部门已正式切换到同一飞书 Base：

- 13 张“机构 X 焦尾 执行表”由机构和内部同事实时协作，是单机构业务事实的首选来源。
- “全量表（AI维护-更新时间）”由豆尼汇总，用于工作台、跨机构横向对比、历史检索和提报查重；它不是机构实时表的替代品。
- 旧机构电子表格只允许用于历史修复和迁移复核，不得覆盖机构已经在多维表修改的新数据。
- 客户审核/发布表、每周执行文档和群消息仍是各自业务动作的权威证据，不能机械迁入多维表后就废弃。

## 工作原则

1. 单机构实时查询读对应机构多维表；跨机构汇总读全量表。需要最新状态且全量表可能延迟时，直接并行读取机构表。
2. 提报、执行、审核、发布是不同证据，不能互相冒充；“提报审核”不代表已执行，只有有效发布记录/链接才能证明已发布。
3. 同名达人可以跨机构或多次合作。优先按唯一 ID、稳定账号/主页、平台、项目/专项和合作时间关联，不能只靠昵称覆盖。
4. 项目分类统一使用 `项目/专项`，不再使用旧“业务线”字段。办公任务统一写工作任务；科普、科普号、科普专项统一写课程教学。
5. 群消息、客户表与多维表冲突时保留证据并说明，不静默覆盖。
6. 机构之间按 Base 高级权限隔离；豆尼读取全量数据不等于可以向机构或客户泄露数据。

## 查询路由

1. 先读 [semantic-contract.md](references/semantic-contract.md)。
2. 识别问题范围：
   - 某达人的“所有数据 / 合作记录 / 发布过什么” → 先查全量表，因为可能跨机构、跨专项、多批次。
   - 某机构中某达人/某合作的最新状态 → 对应机构多维表。
   - 全机构对比、仪表盘、历史调用、查重 → 全量表。
   - 客户审核/发布结论 → 对应客户表实时读取。
   - 当周发布、待审核脚本 → 当周执行文档实时读取。
   - 客户要求、同事承诺、DDL → 群消息和 Deadline 账本。
3. 用户给出明确链接时，当轮优先读链接，不拿旧快照覆盖。
4. 返回达人、主页、脚本、发布内容时使用真实可点击 URL，不输出内部 source ID。
5. 日常业务查询直接调用 `query_lifecycle_base`；`search_synced_facts` 只用于找历史口径和遥师已确认说明，不得抢先回答当前达人/机构/专项/执行/发布数据。

## 回答口径

- 先回答用户真正问的结论，再列必要明细；不展示内部工具名、证据 ID 或调用日志。
- 同名命中多条是正常业务记录。必须分开说：表内记录数、执行中数、已发布数、仅提报/审核数；不把“出现 N 次”直接说成“合作 N 次”。
- 课程教学存在同一达人批量 10 期等常见情况，不得当成重复错误。
- 昵称精确命中时，不要无故要求补主页链接、抖音号或其他身份锚点；只在多个不同人确实同名、且会改变结论时才说明歧义。
- 同一条记录缺少发布链接或效果数据时，仍先返回其他已确认信息，再单独标注缺口；不因一个字段缺失否定整个查询。

## 同步边界

- 每日提报表 → 机构表：只由 `$douni-daily-submission-backfill` 回传已完成客户审核的最新记录，并逐字段回读。
- 机构表 → 全量表：以唯一 ID 定位并完整同步全部可写业务字段；全量表不再持有独立权威字段。
- 全量表 → 机构表：已停用，禁止把全量表当作审核或选题权威源回压机构表。
- 旧电子表格 → Base：仅处理明确的历史修复范围；不再全量覆盖当前协作数据。
- 新增机构表通过稳定名称“机构 X 焦尾 执行表”自动发现，不把易变 table_id 列表散落到每个 Skill。

## 质量门

- 机构表当前基线为 13 张（含自建）；缺表时失败关闭。
- 唯一 ID 在单机构表内不得重复；全量表汇总后也不得重复。
- 字段按名称读取，不依赖视图顺序、列字母或可变表名后缀。
- 链接写入普通 URL，让飞书生成标题；写后验证真实目标可打开。
- 附件保留 file_token，不能只复制附件文件名。
- 低置信度关联不自动合并；问题写入执行记录，不向业务表增加无用审计列。

## 改动边界

- 新增/更名字段时，同时检查机构表、全量表、全量同步、权威字段回填、工作台数据契约和依赖 Skill。
- 新增项目/专项用现有字段选项，不新增“业务线”。
- 客户表写入、外发、机构表写入仍由具体 Skill 的授权决定，本 Skill 不自动扩大权限。
